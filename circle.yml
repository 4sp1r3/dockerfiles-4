machine:
  services:
    - docker

dependencies:
  override:
    - docker pull gliderlabs/alpine

test:
  override:
    - cd nc; bash test.sh
    - cd ../telnet; bash test.sh
    - cd ../lynx; bash test.sh
